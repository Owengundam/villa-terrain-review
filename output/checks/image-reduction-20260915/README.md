# Image-derived villa reduction — 2026-09-15

Open `index.html` for the four alternatives, click any villa to activate/deactivate it, or select its ID and use the button. Inactive units remain ghosted in the viewer. Hover labels appear after 200 ms and are cancelled when the pointer leaves. Red cones indicate failed view/elevation checks; red footprints indicate active clearance or boundary conflicts. Export PNG builds a clean plan from active villas only, including their IDs, contour lines, site boundary and validation status; it excludes ghosts, cones and inspector connectors. The viewer places the plan beside the villa inspector/elevation panel on desktop and stacks them on small screens. Changing arrangement restores that arrangement's verified reduction. Restore plan discards manual toggles for the displayed option.

Elevation comparisons below the plan update with the selected villa and current pad fit. Every footprint intersecting its cone appears, including inactive ghosts. Each pair shows the selected and front villa as 5 m buildings, reference terrain markers, adjusted pads, the maximum front-pad level for a 5.25 m drop, projected forward distance and angular overlap. These are projected elevations with lateral offsets collapsed, not sampled terrain sections. Inactive units do not contribute to the view calculation. The existing PNG button exports the plan. Elevation data/render checks cover all 293 selections at three widths.

| Input | Reconstructed | Retained |
|---|---:|---:|
| Free | 43 | 20 |
| Parallel | 74 | 19 |
| Staggered 2 | 87 | 21 |
| Staggered 3 | 89 | 19 |

Inputs are the named PNGs in `output/previews/ai-layout-configs-20260915`. The first staggered image is excluded. Extraction overlays label every detected unit; connected outlines are closed and filled before extracting minimum-area boxes. Raster centres and axes are registered by the red site-boundary bounds. This is approximate registration: AI boundary/contour distortion is not survey geometry. Every reconstructed footprint is replaced with an exact 11 × 23 m rectangle in the real site coordinates.

The terrain reference is the previously exported live Rhino contour/height-mark data from September 14 at 18:03:35, re-exported and confirmed identical on September 15 at 10:48:06 (`../phase0/real-terrain-20260915-104806.json`). Rhino model units are metres, tolerance 0.001 m. Site area is 40,335.4 m²; XY extents are 242.964 × 286.910 m. The source villa and all 293 reconstructed villas independently measure 11 × 23 m. The field interpolates eight nearby text-height insertion anchors on a 4 m grid; insertion anchors approximate spot XY. Tagged contours are smoothed/inferred. No live Rhino edits were performed.

Rotation starts from the image axis, clamped to ±15° of the nearest local contour normal facing downhill. Downhill sign is now determined across the full 23 m footprint, averaging terrain at three points along each rear/front facade, rather than using a noisy 1 m centre derivative. Every candidate rotation must have lower front terrain than rear terrain. This corrected 30 previously uphill orientations (6 Free, 4 Parallel, 10 Staggered 2, 10 Staggered 3). Four coordinate-descent passes consider the clamped angle and −15, −7.5, 0, 7.5, 15 degrees to reduce geometry conflicts. Centres stay fixed. Boundary failures and high-degree clearance conflicts are deactivated, then view/elevation conflict participants are reduced until feasible. A reinsertion pass recovers units now feasible in the reduced neighbourhood. This is a deterministic local reduction, not a minimum-deletion or maximum-count guarantee.

Rules: no overlap; entire active footprint inside site; side gap >3 m in both local frames whenever depth intervals overlap (3.05 m candidate buffer); central ±15° view at least 70% clear by union of obstruction intervals; blocker vertically clears if its pad is at least 5.25 m lower; pads at most ±1.5 m from reference terrain. Cone drawing and calculations now both cover the full arrangement. The selected villa shows actual blocker IDs, angular widths, pad drops and plan connectors. Orientation tolerance and view-cone angle are distinct rules.

Every manual toggle starts from the current bounded pad heights, without further deletion or movement. The feasible-only local pad search locks currently height-cleared relationships, tries additional clearance edges and retains the best feasible improvement; impossible edges are rejected rather than returning a failed projection. This prevents an unrelated already-passing view from becoming red because of an unsuccessful pad fit. Keeping cleared relationships can restrict the search; it is not a globally optimal pad solution. An unresolved manual state remains visible with actual failures and is not proof that every possible pad arrangement fails.

The original Staggered 3 V035 → V025 complaint was reproduced. V035 is outside V025's cone. The old solver changed V062 and V068's heights so their drops below V025 fell from 6.717 m to 4.464 m and 4.972 m. Their combined angular obstruction then made V025 red. The corrected solver, tested against the archived original layout and its original ±3 m bounds, retains V025's passing view when V035 is reactivated. The current revised layouts use ±1.5 m throughout.

Validation: all four saved states passed the independent geometry checker, exact interval coverage and separate ray/polygon cross-check, plus every-unit edge lengths, downhill facade levels and ±15° orientation checks. JavaScript tests passed 293 individual toggle/toggle-back cases, ±1.5 m heights, unchanged active masks, no new passing-view regressions from pad fitting, all-active view/clearance failures, saved-state recovery, and the original V035/V025 regression. All 15 Python tests pass. Browser interaction and PNG download have not been tested end-to-end; browser access to the network-file preview was unavailable. Static PNGs for all four reduced states are included alongside the interactive viewer.

`report.json` preserves exact geometry, reference and fitted levels, active states, conflicts, angular spans, reduction history and validation. `extracted.json` preserves raster-derived locations and registration metadata. `*-extraction.png` are extraction QA; `*-reduced.png` are static plan previews.
